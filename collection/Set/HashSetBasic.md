
#WHY HASHSET...?

#In java, HashSet is commonly used if we have to access elements randomly.It is because elements in a hash table are accessed using hash code.

#The hashcode of an element is unique identity that helps to identify the element in a hashtable.

#HashSet cannot contain duplicate elements. Hence, ecah hashset element has a unique hashcode